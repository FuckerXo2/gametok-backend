// Generated runtime data — DO NOT EDIT. Kenney 3D models for THIS game are inlined here as base64
// data-URIs and assigned to window.DREAM_MODELS before the game boots (src/bootstrap.ts imports this
// file first). loadModel() in threeAssets.ts resolves keys against window.DREAM_MODELS — no CORS, no
// runtime fetch, fully self-contained after the single-file build.
//
// The forge OVERWRITES this file with the real model blob when models are materialized. This stub is
// the no-models default so the import always resolves and the build stays clean. Keeping the blob in
// THIS bundled module (instead of inlined into index.html) keeps index.html small enough for the
// Phase 2 repair agent to read and edit — the inline-base64-in-HTML version bloated it past 1MB and
// made it unreadable/uneditable, stranding the agent.
;(window as unknown as { DREAM_MODELS?: Record<string, string> }).DREAM_MODELS = {};
export {};
