// Stub — OVERWRITTEN by the maker (materializeKenney2dSprites) when Kenney 2D sprites are selected
// for this game. By default no sprites are provided, so sprite()/animatedSprite()/tile() no-op and the
// game falls back to code-drawn primitives. Imported first by bootstrap.ts so the role map exists
// before assetLoader runs. Mirrors threejs-kernel/src/dreamModels.ts.
;(window as unknown as { DREAM_SPRITE_ROLES?: unknown }).DREAM_SPRITE_ROLES =
  (window as unknown as { DREAM_SPRITE_ROLES?: unknown }).DREAM_SPRITE_ROLES || {};
export {};
